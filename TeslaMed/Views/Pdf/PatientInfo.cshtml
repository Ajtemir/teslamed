@using TeslaMed.ViewModels
@model PatientWithDiagnosticsViewModel
@{
    Layout = null;
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
<link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
<link rel="stylesheet" href="~/TeslaMed.styles.css" asp-append-version="true" />
<style>
    p, li {
        line-height: 1.15;
    }
    .main {
        margin: 0;
        padding: 0;
        line-height: 1.15;
    }
</style>
<div class="align-items-center mb-0 mt-0 pt-0">
    <div class="text-center mb-0 mt-0 pt-0">
        <img src="data:image/jpeg;base64,@Model.Base64Image" alt="Тесламед" width="180" height="90" />
    </div>
    <div style="font-size: 11px" class="fw-bold text-center pt-0 mt-0">
        <p class="mb-1">Жоопкерчилиги чектелген коом "Тесламед", КР ССМ лицензиясы НГМУ №3591, Бишкек ш., Горький, 95 к., тел.: +996 502 56 34 00</p>
        <p>Общество с ограниченной ответсвтенностью "Тесламед", лицензия МЗ КР НГМУ №3591 г. Бишкек, ул. Горького 95, тел.: +996 502 56 34 00</p>
    </div>
</div>

<div class="main">
<h3 class="text-center fw-bold">№ @Model.Patient.Code</h3>
    <p class="mb-1"><span class="fw-bold">ФИО пациента:</span> @Model.Patient.Surname @Model.Patient.Name</p>
    <div class="me-3">
        <div class="row">
            <div class="col">
                <p class="float-start mb-1"><span class="fw-bold">Дата рождения:</span> @Model.Patient.BirthDate.ToString("dd.MM.yyyy")</p>
            </div>
            <div class="col">
                <p class="float-end mb-1"><span class="fw-bold">Дата посещения:</span> @Model.Diagnostics.TimeArrival.ToString("dd.MM.yyyy HH:mm")</p>
            </div>
        </div>
    </div>
    <p class="text-white mb-1">some text</p>
    <p class="mb-1"><span class="fw-bold">Пол:</span> @Model.Patient.Gender</p>
    <p class="mb-1"><span class="fw-bold">Номер телефона:</span> @Model.Patient.PhoneNumber</p>
    <p class="mb-1"><span class="fw-bold">Способ исследования:</span> @Model.ResearchMethod.Name</p>
    <p class="fw-bold mb-1">Вид исследования:</p>
    <ol class="ms-4 mb-1">
        @foreach (var type in Model.Diagnostics.TypesOfDiagnostics)
        {
            <li>@Model.ResearchMethod.Name @type.Name</li>
        }
    </ol>
    <p class="mb-1"><span class="fw-bold">Способ прибытия:</span> @Model.Diagnostics.ArrivalType.Name</p>
    <p class="mb-1">
        <span class="fw-bold">Направлен от врача:</span>
        @if (Model.Diagnostics.ArrivalTypeDoctor == null)
        {
            <span>-</span>
        }
        else
        {
            <text>
                @Model.Diagnostics.ArrivalTypeDoctor.Name&nbsp;@Model.Diagnostics.ArrivalTypeDoctor.Surname
            </text>
        }
    </p>
    <p class="mb-1"><span class="fw-bold">Врач:</span> @Model.Diagnostics.Doctor.Name @Model.Diagnostics.Doctor.Surname</p>
    <div class="border-top-1 border border-dark mb-1"></div>
    <p class="mb-2"><span class="fw-bold">Анамнез:</span> @Model.Diagnostics.Anamnesis</p>

    <div class="text-center mt-1">
        <p class="fw-bold mb-1" style="font-size: 12px">Результаты вы можете найти на сайте https://result.teslamed.kg по номеру вашего исследования</p>
        <p class="mb-1" style="font-size: 12px">Внимание! Воспользовавшись услугами нашего диагностического центра, Вы, одновременно с Публичной офертой и Политикой конфиденциальности соглашаетесь с политикой обработки персональных данных и политикой конфиденциальности.</p>
        <p style="font-size: 12px">Спасибо за оказанное доверие, желаем крепкого здоровья!</p>
    </div>
</div>
